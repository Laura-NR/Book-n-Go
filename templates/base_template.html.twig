<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="./node_modules/bootstrap/dist/css/bootstrap.css">
    {% block link %} {% endblock %}
    <script src="./node_modules/bootstrap/dist/js/bootstrap.bundle.js"></script>
    
    <meta name="description" content="{%if description is defined %} {{description}} {% else %} {{constant('WEBSITE_DESCRIPTION')}} {% endif %}">
    {% block css %} {% endblock %}
    <title>{% block title %}{{ constant('WEBSITE_TITLE')}}{% endblock %}</title>
</head>

{# faire en sorte que le contenu prenne toute la fenetre en empilant le contenu de body sur 100% de l'ecran #}
<body class="d-flex flex-column min-vh-100">
    <header class="bg-body-tertiary">
        <nav class="navbar navbar-expand-lg container">
            <div class="container-fluid">
                <a class="navbar-brand" href="index.php">
                    <img src="./images/logo3_OK_ORIGINAL.png" alt="Logo" width="30" height="24" class="d-inline-block align-text-top">
                    {{ constant('WEBSITE_TITLE')}} {{ constant('WEBSITE_VERSION')}}
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        {% if session.user_id %}
                        <li class="nav-item btn btn-primary rounded-pill me-2">
                            <a class="nav-link" href="index.php?controleur=utilisateur&methode=afficherDashboard">Dashboard</a>
                        </li>
                        <li class="nav-item btn btn-primary rounded-pill me-2">
                            <a class="nav-link" href="index.php?controleur=utilisateur&methode=deconnexion">Déconnexion</a>
                        </li>
                        {% else %}
                        <li class="nav-item btn btn-primary rounded-pill me-2">
                            <a class="nav-link" href="index.php?controleur=utilisateur&methode=afficherInscription">S'inscrire</a>
                        </li>
                        <li class="nav-item btn btn-primary rounded-pill me-2">
                            <a class="nav-link" href="index.php?controleur=utilisateur&methode=afficherConnexion">Connexion</a>
                        </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    {# faire en sorte que le contenu du main s'étende sur toute la place disponible #}
    <main class="flex-grow-1">
        {% block sidebar %}
        {% endblock %}
        {% block dashborad_content %}
            {% block content %}
            {% endblock %}
        {% endblock %}
    </main>
   
    <footer class="bg-body-tertiary py-5">
        <div class="container text-center">
            <div class="row">
                <div class="col align-items-center justify-content-center">
                    <div class="text-center">
                        <a href="#">Mentions légales</a>
                    </div>
                    <div class="text-center">
                        <a href="#">Aide</a>
                    </div>
                </div>
                <div class="col align-items-center justify-content-center">
                    <img src="#" alt="LOGO_BOOKNGO">
                </div>
                <div class="col align-items-center justify-content-center">
                    <div class="text-center">
                        <a href="#">Cookie Policy</a>
                    </div>
                    <div class="text-center">
                        <a href="#">Newsletter</a>
                    </div>
                </div>
            </div>
        </div>
    </footer>
    {% block script %} {% endblock %}
</body>

</html>



