{% extends "base_template.html.twig" %}
{% block title %}{{constant('WEBSITE_TITLE')}} Page d'accueil{% endblock %}

{% block navbar_items %}
    <li class="nav-item btn btn-primary rounded-pill me-2">
        <a class="nav-link" href="#">Devenir guide</a>
    </li>
    <li class="nav-item btn btn-primary rounded-pill me-2">
        <a class="nav-link" href="#">Déconnexion</a>
    </li>
{% endblock %}

{% block content %}

    <div class="container text-center">
        <div class="row row-cols-4 align-items-center">
    {% for carnet in carnets %}
            <div class="card col mb-3 mx-1">
                <img src="./images/carnet/{{ carnet.chemin_img }}" class="card-img-top" alt="#">
                <div class="card-body">
                    <h5 class="card-title">{{ carnet.titre }}</h5>
                    <a href="?controleur=carnetVoyage&methode=afficher&id={{ carnet.id }}" class="btn btn-primary">Consulter</a>
                </div>
            </div>
    {% endfor %}
        </div>
    </div>

{% endblock %}